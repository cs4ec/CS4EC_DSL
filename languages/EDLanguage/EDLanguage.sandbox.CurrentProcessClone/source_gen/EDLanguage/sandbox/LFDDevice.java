package EDLanguage.sandbox;

/*Generated by MPS */

import simcore.action.Behaviour;
import repast.simphony.space.continuous.ContinuousSpace;
import repast.simphony.space.grid.Grid;
import simcore.Signals.Signal;
import simcore.action.BehaviourStep;
import simcore.diagnosis.TestResult;
import java.util.ArrayList;
import simcore.basicStructures.Board;
import simcore.Signals.DirectSignal;

public class LFDDevice extends Staff {

  public double groupStress = Double.parseDouble("" + "0");
  public Behaviour behaviourBuilder;

  public LFDDevice(ContinuousSpace<Object> space, Grid<Object> grid) {
    super(space, grid);
    mintMyMaxPatients = 0;
  }

  public LFDDevice(ContinuousSpace<Object> space, Grid<Object> grid, String pstrStartLocation) {
    super(space, grid, pstrStartLocation);
  }

  public Behaviour BuildActionFromSignal(Signal s) {
    switch (s.getName()) {
      case "":
        break;
      case "ConductLFD":
        behaviourBuilder = new Behaviour("ConductLFD");
        this.InitTestPatient(s);
        break;
      case "ConductTrackAndTraceLFD":
        behaviourBuilder = new Behaviour("ConductTrackAndTraceLFD");
        this.InitTestPatientTrackAndTrace(s);
        break;
      default:
        System.out.println("Set mission: " + s.getName() + " failed!");
        return behaviourBuilder;
    }
    return behaviourBuilder;
  }

  public class TestAction_a0a extends BehaviourStep {
    /*package*/ Behaviour behaviour;
    /*package*/ int testingTime = SURESCREEN.getInstance().getProcessingTime();
    /*package*/ int timeExecuted = 0;
    /*package*/ TestResult testResult;
    public TestAction_a0a(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      if (timeExecuted == 0) {
        testResult = SURESCREEN.getInstance().TestPatient(behaviour.getPatient());
      }

      timeExecuted++;

      if (timeExecuted == testingTime) {
        if (testResult.isInfected()) {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0a(behaviour));
          behaviour.injectSteps(plstSteps);
        } else {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0a_3(behaviour));
          behaviour.injectSteps(plstSteps);
        }
      }
    }

    public boolean finishCondition() {
      return timeExecuted == testingTime;
    }
  }
  public class TestAction_a0a_1 extends BehaviourStep {
    /*package*/ Behaviour behaviour;
    /*package*/ int testingTime = SURESCREEN.getInstance().getProcessingTime();
    /*package*/ int timeExecuted = 0;
    /*package*/ TestResult testResult;
    public TestAction_a0a_1(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      if (timeExecuted == 0) {
        testResult = SURESCREEN.getInstance().TestPatient(behaviour.getPatient());
      }

      timeExecuted++;

      if (timeExecuted == testingTime) {
        if (testResult.isInfected()) {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0a(behaviour));
          behaviour.injectSteps(plstSteps);
        } else {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0a_3(behaviour));
          behaviour.injectSteps(plstSteps);
        }
      }
    }

    public boolean finishCondition() {
      return timeExecuted == testingTime;
    }
  }
  public class SendSignalAction_a0a0a extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0a(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDCompleteSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class SendSignalAction_a0a0a_1 extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0a_1(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDCompleteSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class SendSignalAction_a0a0a_3 extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0a_3(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDCompleteSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class SendSignalAction_a0a0a_5 extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0a_5(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDCompleteSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class TestAction_a0b extends BehaviourStep {
    /*package*/ Behaviour behaviour;
    /*package*/ int testingTime = SURESCREEN.getInstance().getProcessingTime();
    /*package*/ int timeExecuted = 0;
    /*package*/ TestResult testResult;
    public TestAction_a0b(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      if (timeExecuted == 0) {
        testResult = SURESCREEN.getInstance().TestPatient(behaviour.getPatient());
      }

      timeExecuted++;

      if (timeExecuted == testingTime) {
        if (testResult.isInfected()) {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0b(behaviour));
          behaviour.injectSteps(plstSteps);
        } else {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0b_3(behaviour));
          behaviour.injectSteps(plstSteps);
        }
      }
    }

    public boolean finishCondition() {
      return timeExecuted == testingTime;
    }
  }
  public class TestAction_a0b_1 extends BehaviourStep {
    /*package*/ Behaviour behaviour;
    /*package*/ int testingTime = SURESCREEN.getInstance().getProcessingTime();
    /*package*/ int timeExecuted = 0;
    /*package*/ TestResult testResult;
    public TestAction_a0b_1(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      if (timeExecuted == 0) {
        testResult = SURESCREEN.getInstance().TestPatient(behaviour.getPatient());
      }

      timeExecuted++;

      if (timeExecuted == testingTime) {
        if (testResult.isInfected()) {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0b(behaviour));
          behaviour.injectSteps(plstSteps);
        } else {
          ArrayList<BehaviourStep> plstSteps = new ArrayList();
          plstSteps.add(new SendSignalAction_a0a0b_3(behaviour));
          behaviour.injectSteps(plstSteps);
        }
      }
    }

    public boolean finishCondition() {
      return timeExecuted == testingTime;
    }
  }
  public class SendSignalAction_a0a0b extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0b(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDTrackAndTraceSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class SendSignalAction_a0a0b_1 extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0b_1(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDTrackAndTraceSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class SendSignalAction_a0a0b_3 extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0b_3(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDTrackAndTraceSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }
  public class SendSignalAction_a0a0b_5 extends BehaviourStep {
    /*package*/ Behaviour behaviour;

    public SendSignalAction_a0a0b_5(Behaviour behaviour) {
      this.behaviour = behaviour;
    }

    public void execute() {
      Board b = ReadBoard();
      Signal sendSignalTemp = new Signal();
      sendSignalTemp = new LFDTrackAndTraceSignal();
      ((DirectSignal) sendSignalTemp).setTarget(behaviour.getSignalTrigger().GetData("replyTo"));
      sendSignalTemp.AddData("patient", behaviour.getSignalTrigger().GetData("patient"));

      b.PushMission(sendSignalTemp);
    }
  }


  public void InitTestPatient(Signal s) {
    behaviourBuilder.setSignalTrigger(s);
    ArrayList<BehaviourStep> plstSteps = new ArrayList();
    plstSteps.add(new TestAction_a0a(behaviourBuilder));
    behaviourBuilder.setSteps(plstSteps);

    Signal sendSignalTemp = new Signal();

  }
  public void InitTestPatientTrackAndTrace(Signal s) {
    behaviourBuilder.setSignalTrigger(s);
    ArrayList<BehaviourStep> plstSteps = new ArrayList();
    plstSteps.add(new TestAction_a0b(behaviourBuilder));
    behaviourBuilder.setSteps(plstSteps);

    Signal sendSignalTemp = new Signal();

  }

}
